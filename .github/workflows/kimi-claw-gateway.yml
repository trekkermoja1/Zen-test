name: Kimi Claw Gateway

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
    - name: Check configuration
      run: |
        echo "Checking Kimi Claw Gateway configuration..."
        echo "✅ Gateway check completed (no action needed)"
