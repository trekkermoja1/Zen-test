name: Welcome New Contributors

on:
  pull_request_target:
    types: [opened]
  issues:
    types: [opened]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  issues: write
  pull-requests: write

jobs:
  welcome:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Welcome new contributor on PR
        if: github.event_name == 'pull_request_target'
        uses: actions/github-script@v8
        with:
          script: |
            const creator = context.payload.pull_request.user.login;
            const repo = context.repo;
            
            const { data: prs } = await github.rest.pulls.list({
              owner: repo.owner,
              repo: repo.repo,
              state: 'all',
              creator: creator
            });
            
            if (prs.length === 1) {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: repo.owner,
                repo: repo.repo,
                body: `Welcome @${creator}! 🎉 Thank you for your first pull request!\n\nWe appreciate your contribution and will review it soon. If you have any questions, feel free to ask.`
              });
            }

      - name: Welcome new contributor on Issue
        if: github.event_name == 'issues'
        uses: actions/github-script@v8
        with:
          script: |
            const creator = context.payload.issue.user.login;
            const repo = context.repo;
            
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: repo.owner,
              repo: repo.repo,
              state: 'all',
              creator: creator
            });
            
            if (issues.length === 1) {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: repo.owner,
                repo: repo.repo,
                body: `Welcome @${creator}! 🎉 Thank you for opening your first issue!\n\nWe value your feedback and will look into it soon.`
              });
            }
