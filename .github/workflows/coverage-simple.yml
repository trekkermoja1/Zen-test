# DEAKTIVIERT - Verwendet ci.yml oder codecov.yml stattdessen
# Dieser Workflow lief nur tests/test_standalone.py welches nicht existiert
# oder keine Projekt-Imports macht.
#
# Verwende stattdessen: ci.yml oder codecov.yml
name: Simple Coverage (DISABLED)

on:
  # DISABLED: Erzeugt falsche Coverage
  # push:
  #   branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Skipped
      run: echo "This workflow is disabled - use ci.yml instead"
