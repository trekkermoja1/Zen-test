# COVERAGE DISABLED - Use codecov.yml instead
# This workflow has been disabled to avoid overwriting coverage with incorrect values
name: Test Coverage (DISABLED)

on:
  # DISABLED to prevent coverage overwrite
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
    - name: Skipped
      run: echo "This workflow is disabled. Use codecov.yml for coverage."
