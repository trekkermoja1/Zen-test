# DEAKTIVIERT - Verwendet tests/ stattdessen
# Dieser Workflow erzeugte 0% Coverage weil er nur test_coverage_quick.py lief
# welches keine Projekt-Dateien importiert.
#
# Verwende stattdessen: ci.yml oder codecov.yml
name: Quick Coverage Boost (DISABLED)

on:
  # DISABLED: Erzeugt falsche 0% Coverage
  # push:
  #   branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Skipped
      run: echo "This workflow is disabled - use ci.yml instead"
