name: Deploy Documentation to GitHub Pages

on:
  push:
    branches: [master, main]
    paths:
      - 'docs/**'
      - 'README.md'
      - 'mkdocs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install MkDocs
        run: |
          pip install mkdocs-material
          pip install mkdocstrings[python]
          pip install mkdocs-minify-plugin

      - name: Create MkDocs config if not exists
        run: |
          if [ ! -f mkdocs.yml ]; then
            echo "site_name: Zen AI Pentest" > mkdocs.yml
            echo "site_description: AI-Powered Penetration Testing Framework" >> mkdocs.yml
            echo "site_author: SHAdd0WTAka" >> mkdocs.yml
            echo "site_url: https://shadd0wtaka.github.io/zen-ai-pentest" >> mkdocs.yml
            echo "" >> mkdocs.yml
            echo "repo_name: SHAdd0WTAka/zen-ai-pentest" >> mkdocs.yml
            echo "repo_url: https://github.com/SHAdd0WTAka/zen-ai-pentest" >> mkdocs.yml
            echo "" >> mkdocs.yml
            echo "theme:" >> mkdocs.yml
            echo "  name: material" >> mkdocs.yml
            echo "" >> mkdocs.yml
            echo "plugins:" >> mkdocs.yml
            echo "  - search" >> mkdocs.yml
          fi

      - name: Create index.md from README
        run: |
          if [ ! -f docs/index.md ]; then
            cp README.md docs/index.md
          fi

      - name: Build site
        run: mkdocs build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
